"use client";

import { Eye, EyeOff, Lock } from "lucide-react";
import { useState } from "react";
import type { AuthFieldErrors, PasswordStrength } from "@/types/auth";

export function PasswordInput({ id = "password", label = "Password", value, onChange, error, showStrength, strength, autoComplete = "current-password" }: { id?: string; label?: string; value: string; onChange: (value: string) => void; error?: AuthFieldErrors["password"] | AuthFieldErrors["confirmPassword"]; showStrength?: boolean; strength?: PasswordStrength; autoComplete?: string }) { const [shown, setShown] = useState(false); const tone = ["bg-rose-500", "bg-orange-500", "bg-amber-500", "bg-emerald-500", "bg-emerald-600"][strength?.score ?? 0]; return <div><label htmlFor={id} className="text-xs font-bold text-slate-700 dark:text-slate-200">{label}</label><div className="relative mt-2"><Lock aria-hidden="true" size={16} className="pointer-events-none absolute left-3 top-1/2 -translate-y-1/2 text-slate-400"/><input id={id} name={id} type={shown ? "text" : "password"} autoComplete={autoComplete} value={value} onChange={e => onChange(e.target.value)} aria-invalid={Boolean(error)} aria-describedby={error ? `${id}-error` : undefined} placeholder="••••••••" className="min-h-11 w-full rounded-xl border border-slate-200 bg-white py-2 pl-10 pr-11 text-sm outline-none transition focus:border-indigo-500 focus:ring-4 focus:ring-indigo-100 dark:border-white/10 dark:bg-white/5 dark:text-white dark:focus:ring-indigo-500/20"/><button type="button" onClick={() => setShown(!shown)} aria-label={shown ? "Hide password" : "Show password"} className="absolute right-2 top-1/2 grid h-8 w-8 -translate-y-1/2 place-items-center rounded-lg text-slate-400 hover:text-slate-600 dark:hover:text-slate-200">{shown ? <EyeOff size={17}/> : <Eye size={17}/>}</button></div>{showStrength && value && strength && <div className="mt-2"><div className="flex gap-1">{[1,2,3,4].map(i=><span key={i} className={`h-1 flex-1 rounded-full ${i <= strength.score ? tone : "bg-slate-200 dark:bg-white/10"}`}/>)}</div><p className="mt-1 text-[11px] text-slate-500">Strength: <b className="text-slate-700 dark:text-slate-200">{strength.label}</b></p></div>}{error && <p id={`${id}-error`} className="mt-1.5 text-xs font-medium text-rose-600">{error}</p>}</div>; }
