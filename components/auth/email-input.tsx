import { Mail } from "lucide-react";
import type { AuthFieldErrors } from "@/types/auth";

export function EmailInput({ value, onChange, error }: { value: string; onChange: (value: string) => void; error?: AuthFieldErrors["email"] }) { return <div><label htmlFor="email" className="text-xs font-bold text-slate-700 dark:text-slate-200">Email address</label><div className="relative mt-2"><Mail aria-hidden="true" size={16} className="pointer-events-none absolute left-3 top-1/2 -translate-y-1/2 text-slate-400"/><input id="email" name="email" type="email" autoComplete="email" value={value} onChange={e => onChange(e.target.value)} aria-invalid={Boolean(error)} aria-describedby={error ? "email-error" : undefined} placeholder="you@example.com" className="min-h-11 w-full rounded-xl border border-slate-200 bg-white py-2 pl-10 pr-3 text-sm outline-none transition placeholder:text-slate-400 focus:border-indigo-500 focus:ring-4 focus:ring-indigo-100 dark:border-white/10 dark:bg-white/5 dark:text-white dark:focus:ring-indigo-500/20"/>{error && <p id="email-error" className="mt-1.5 text-xs font-medium text-rose-600">{error}</p>}</div></div>; }
